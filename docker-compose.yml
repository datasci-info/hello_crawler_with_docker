version: '2'

services:
  mongodb:
    image: 'bitnami/mongodb:latest'
    ports:
      - "27017:27017"
    environment:
      - MONGODB_USERNAME=elesupplier
      - MONGODB_PASSWORD=1qaz@WSX
      - MONGODB_DATABASE=elesupplier
